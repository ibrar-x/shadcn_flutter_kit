// Backward-compatible shim.
//
// FilterBar controller now lives in `filter_bar_logic.dart` so logic can be
// used independently from the FilterBar UI widgets.
export '../../filter_bar_logic.dart' show FilterBarController;
