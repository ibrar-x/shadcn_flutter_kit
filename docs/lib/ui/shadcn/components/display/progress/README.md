# Progress

Linear progress indicator with normalized ranges and theme overrides.

```dart
Progress(progress: 0.6, min: 0, max: 10);
```

Wrap a subtree with `ComponentTheme<ProgressTheme>` to customize colors or border radius.
