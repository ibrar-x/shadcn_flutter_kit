part of '../../form.dart';

/// _FormEntryCachedValue represents a form-related type in the registry.
class _FormEntryCachedValue {
  /// Current value stored for `value`.
  Object? value;

  _FormEntryCachedValue(this.value);
}
