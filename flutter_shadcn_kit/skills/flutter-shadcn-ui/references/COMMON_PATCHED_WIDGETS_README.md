# Common Patched Widgets

Compatibility entry for the previous skill layout.

Use:

- `replacement-map.md` for Material/Cupertino to registry replacements.
- `component-catalog.md` for the full component list.
- `composition.md` for import conflict and overlay rules.

Do not create custom Material wrappers before checking those references.
