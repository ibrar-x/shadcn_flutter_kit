# Forms Rules

Use `references/forms.md` for the current full rules.

Install form stacks together, keep validation in form primitives, and verify with:

```bash
flutter_shadcn validate --json
flutter_shadcn audit --json
flutter_shadcn deps --json
```
