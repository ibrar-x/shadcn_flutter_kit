# Namespace Rules

Use explicit `@namespace/component` IDs in scripts and AI output.

```bash
flutter_shadcn registries --json
flutter_shadcn default shadcn
flutter_shadcn add @shadcn/button
```

Unqualified names can be ambiguous across enabled registries.
