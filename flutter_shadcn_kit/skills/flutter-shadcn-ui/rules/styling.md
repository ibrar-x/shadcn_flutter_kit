# Styling Rules

Use `references/theming-assets.md` for the current full rules.

Prefer CLI theme flows and semantic theme tokens:

```bash
flutter_shadcn theme --list
flutter_shadcn theme --apply modern-minimal
flutter_shadcn sync
flutter_shadcn validate --json
```
