// GENERATED: component theming architecture config file.
// Wires defaults + tokens to theme registry.

import 'spinner_theme_defaults.dart';
import 'spinner_theme_tokens.dart';

class SpinnerThemeConfig {
  const SpinnerThemeConfig._();

  /// Global component theme override for `SpinnerTheme`.
  static Object? global;
  static const String globalType = 'SpinnerTheme';
  static const SpinnerThemeConfigGlobalDefaults globalDefaults =
      SpinnerThemeConfigGlobalDefaults();
  static const SpinnerThemeConfigGlobalTokens globalTokens =
      SpinnerThemeConfigGlobalTokens();
}
