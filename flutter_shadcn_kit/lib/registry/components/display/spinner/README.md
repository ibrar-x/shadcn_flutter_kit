# Spinner

Base spinner helpers that resolve color and size from `ComponentTheme<SpinnerTheme>`.

Wrap specific spinner implementations (dots, arcs, etc.) with `ComponentTheme<SpinnerTheme>` to override defaults.
