// GENERATED: component theming architecture config file.
// Wires defaults + tokens to theme registry.

import 'progress_theme_defaults.dart';
import 'progress_theme_tokens.dart';

class ProgressThemeConfig {
  const ProgressThemeConfig._();

  /// Global component theme override for `ProgressTheme`.
  static Object? global;
  static const String globalType = 'ProgressTheme';
  static const ProgressThemeConfigGlobalDefaults globalDefaults =
      ProgressThemeConfigGlobalDefaults();
  static const ProgressThemeConfigGlobalTokens globalTokens =
      ProgressThemeConfigGlobalTokens();
}
