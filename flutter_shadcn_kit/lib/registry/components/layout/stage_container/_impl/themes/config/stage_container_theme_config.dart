// GENERATED: component theming architecture config file.
// Wires defaults + tokens to theme registry.

import 'stage_container_theme_defaults.dart';
import 'stage_container_theme_tokens.dart';

class StageContainerThemeConfig {
  const StageContainerThemeConfig._();

  /// Global component theme override for `StageContainerTheme`.
  static Object? global;
  static const String globalType = 'StageContainerTheme';
  static const StageContainerThemeConfigGlobalDefaults globalDefaults =
      StageContainerThemeConfigGlobalDefaults();
  static const StageContainerThemeConfigGlobalTokens globalTokens =
      StageContainerThemeConfigGlobalTokens();
}
