# Scaffold

Layout scaffold with headers, footers, and loading indicators.

## Usage
```dart
Scaffold(
  headers: [Text('Header')],
  footers: [Text('Footer')],
  loadingProgress: 0.6,
  child: Center(child: Text('Body')),
)
```
