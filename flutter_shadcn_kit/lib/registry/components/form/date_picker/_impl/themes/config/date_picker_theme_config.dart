// GENERATED: component theming architecture config file.
// Wires defaults + tokens to theme registry.

import 'date_picker_theme_defaults.dart';
import 'date_picker_theme_tokens.dart';

class DatePickerThemeConfig {
  const DatePickerThemeConfig._();

  /// Global component theme override for `DatePickerTheme`.
  static Object? global;
  static const String globalType = 'DatePickerTheme';
  static const DatePickerThemeConfigGlobalDefaults globalDefaults =
      DatePickerThemeConfigGlobalDefaults();
  static const DatePickerThemeConfigGlobalTokens globalTokens =
      DatePickerThemeConfigGlobalTokens();
}
