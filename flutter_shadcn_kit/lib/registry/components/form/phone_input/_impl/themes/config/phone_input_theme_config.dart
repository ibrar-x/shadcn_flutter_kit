// GENERATED: component theming architecture config file.
// Wires defaults + tokens to theme registry.

import 'phone_input_theme_defaults.dart';
import 'phone_input_theme_tokens.dart';

class PhoneInputThemeConfig {
  const PhoneInputThemeConfig._();

  /// Global component theme override for `PhoneInputTheme`.
  static Object? global;
  static const String globalType = 'PhoneInputTheme';
  static const PhoneInputThemeConfigGlobalDefaults globalDefaults =
      PhoneInputThemeConfigGlobalDefaults();
  static const PhoneInputThemeConfigGlobalTokens globalTokens =
      PhoneInputThemeConfigGlobalTokens();
}
