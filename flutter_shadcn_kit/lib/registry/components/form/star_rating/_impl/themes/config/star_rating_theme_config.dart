// GENERATED: component theming architecture config file.
// Wires defaults + tokens to theme registry.

import 'star_rating_theme_defaults.dart';
import 'star_rating_theme_tokens.dart';

class StarRatingThemeConfig {
  const StarRatingThemeConfig._();

  /// Global component theme override for `StarRatingTheme`.
  static Object? global;
  static const String globalType = 'StarRatingTheme';
  static const StarRatingThemeConfigGlobalDefaults globalDefaults =
      StarRatingThemeConfigGlobalDefaults();
  static const StarRatingThemeConfigGlobalTokens globalTokens =
      StarRatingThemeConfigGlobalTokens();
}
