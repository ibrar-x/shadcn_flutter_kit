# Image

A tiny placeholder to include shared image helpers even when no assets are bundled.

```dart
import 'ui/shadcn/components/image/image.dart';

const imagePlaceholder = ImageComponent();
```

No shared files required beyond the registry default.
