# `pagination`

`Pagination` exposes a themed row of controls for navigating paged content. It renders previous/next arrows, optional first/last jumps, page number buttons, and uses `MoreDots` for ellipsis gaps. Install it when a dataset needs explicit paging controls with optional labels or reduced button counts.
