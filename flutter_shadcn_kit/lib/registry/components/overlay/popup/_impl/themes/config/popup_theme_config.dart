// GENERATED: component theming architecture config file.
// Wires defaults + tokens to theme registry.

import 'popup_theme_defaults.dart';
import 'popup_theme_tokens.dart';

class PopupThemeConfig {
  const PopupThemeConfig._();

  /// Global component theme override for `MenuPopupTheme`.
  static Object? global;
  static const String globalType = 'MenuPopupTheme';
  static const PopupThemeConfigGlobalDefaults globalDefaults =
      PopupThemeConfigGlobalDefaults();
  static const PopupThemeConfigGlobalTokens globalTokens =
      PopupThemeConfigGlobalTokens();
}
