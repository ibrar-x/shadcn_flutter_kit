# Debug helpers

Utilities that aid layout debugging:

- `debugPostSticker` draws a translucent overlay box for a few seconds.
- `Widget.debugContainer` can wrap any widget with a debug-colored container when `kDebugContainerVisible` is true.

Enable/disable via `kDebugStickerVisible` and `kDebugContainerVisible`.
