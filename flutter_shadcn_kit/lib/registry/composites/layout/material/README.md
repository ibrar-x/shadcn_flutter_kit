# Material

Composite export that bundles ShadcnUI with common layout + button primitives.

## Usage

```dart
import 'ui/shadcn/composites/material/material.dart';

ShadcnUI(
  child: Card(
    child: PrimaryButton(
      onPressed: () {},
      child: const Text('Continue'),
    ),
  ),
);
```
